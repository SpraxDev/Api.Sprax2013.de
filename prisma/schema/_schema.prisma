generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["views", "tracing", "prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
